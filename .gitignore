node_modules/
dist/
.env
.env.local
*.local
.DS_Store
.wrangler/
worker/node_modules/
data/cache/
.anisearch*.log
.tmdb.log
# Protokolle langer Abrufläufe. Sie sind Arbeitsmaterial, kein Datenbestand —
# das Ergebnis landet in data/*.json, und `pipeline/recover-cr-dub.ts` kann aus
# so einem Protokoll notfalls den Datensatz zurückgewinnen.
crdub-*.log
katalog-*.log

# Von tsc erzeugt. Lag bis zum 21.08.2026 im Repo und war der einzige
# Konfliktpunkt beim Merge des Crunchyroll-Umbaus — eine generierte Datei,
# die zwei Zweige zwangsläufig verschieden schreiben.
tsconfig.tsbuildinfo

# Gepackte Erweiterung und ihr privater Schluessel. Die .pem ist die Identitaet
# der Erweiterung — wer sie hat, kann Updates unter derselben Kennung ausliefern.
extension.crx
extension.pem
